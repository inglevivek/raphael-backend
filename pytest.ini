[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: Integration tests
    unit: Unit tests
    slow: Slow-running tests
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
